<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimary"
    android:gravity="center_vertical"
    android:orientation="vertical"
    android:paddingEnd="@dimen/activityLoginLayoutPadding"
    android:paddingStart="@dimen/activityLoginLayoutPadding"
    tools:context=".activity.LoginActivity">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/activityLoginLogoBottomMargin"
        android:adjustViewBounds="true"
        android:contentDescription="@string/logoImageDescription"
        android:src="@drawable/logo" />

    <android.support.design.widget.TextInputLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/activityLoginEmailEditTextBottomMargin"
        android:theme="@style/loginWidgetEditTextStyle"
        app:hintEnabled="false">

        <EditText
            android:id="@+id/emailEdit"
            style="@style/loginEditTextStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/loginEmailHint"
            android:inputType="textEmailAddress"
            android:maxLength="60"
            android:maxLines="1"
            tools:text="crazy_boy34" />
    </android.support.design.widget.TextInputLayout>

    <android.support.design.widget.TextInputLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/activityLoginPasswordEditTextBottomMargin"
        android:theme="@style/loginWidgetEditTextStyle"
        app:hintEnabled="false">

        <EditText
            android:id="@+id/passwordEdit"
            style="@style/loginEditTextStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/loginPasswordHint"
            android:inputType="textPassword"
            android:maxLength="24"
            android:maxLines="1" />
    </android.support.design.widget.TextInputLayout>

    <Button
        android:id="@+id/loginButton"
        style="@style/loginButtonStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/loginButtonText" />
</LinearLayout>